# Internationalization Foundation Progress

Scope: the fork's Electron/React app only. Preserve English; do not ship Chinese or a test locale.

## Decisions And Evidence

- Initial checkout: `main`, one commit ahead of origin, clean worktree.
- Environment: Linux/WSL2. Use `/home/lilis/.nvm/versions/node/v22.20.0/bin` on PATH
  (Node 22.20.0, pnpm 9.12.1). The default shell uses Node 24 and cannot find pnpm.
- Research: Excalidraw's `packages/excalidraw/i18n.ts` and contributing docs;
  Joplin's `packages/lib/locale.ts` and localisation docs. Details will live in
  `docs/internationalization.md`.
- Choose i18next with locally bundled English source resources, stable semantic IDs,
  shared pure formatting/resolution helpers, and separate main/renderer instances.
- First migration: shell/navigation, Settings (including embedded panels), custom native
  menus, and Trash restore/delete flow. Remaining screens are a documented second phase.
- Main resolves the saved preference and system locale. Renderer initializes from the
  typed bridge before mounting; saved language changes rebuild custom menus and notify UI.

## Work

- [x] Read task attachment, root/scoped instructions, settings/IPC/menu/render entry.
- [x] Research two open-source implementations and i18next official documentation.
- [x] Shared resources, registry, resolution, formats, resource/type checks.
- [x] Saved preference and main/renderer coordination.
- [x] First UI migration and longer-language layout checks.
- [x] Focused tests, Electron restart/fallback/pseudo coverage, production artifacts.
- [x] Root lint, typecheck, test; final diff review and developer guide.

## Verification

- Final concentrated tests: 13 files / 613 tests passed (resources, persistence,
  settings, IPC, preload, bootstrap, migration and undo).
- Root lint passed: 1154 files. Final root typecheck passed: all 15 workspace packages.
- Real Linux Electron (WSLg `DISPLAY=:0`): saved language restart and unsupported-language
  fallback passed. Dedicated pseudo build: Settings at 1440/1024, shortcut panel, Trash
  confirmation/restore, menu fallback and restart passed after fixing long-text overflow.
- Production renderer/main builds passed. `pnpm i18n:check-build` confirmed English
  in main and both renderer artifacts, with the test locale absent.
- Final pseudo Electron build: 2/2 tests passed; production Electron regression:
  27 passed, 2 intentionally skipped (macOS-only window behavior; pseudo suite covered
  separately). Covers Settings, Trash/undo, desktop security, backup and file restore.
- Final English screenshot/accessibility/restart check passed. Menu, Windows paths,
  Windows ONNX and native packaging-branch unit tests: 23/23 passed.
- Initial bare `pnpm test`: 450/460 files and 4902/4931 tests passed. New vocabulary,
  settings-default and bootstrap-mock failures were fixed. The chronic-postpone fixture
  used July 2026 as a future date; reproduced on clean HEAD `1444e9e` in
  `/tmp/interleave-i18n-baseline` and fixed by pinning its clock. QueueScreen had pending
  one-shot mock results between tests; resetting that command mock makes all 57 pass.
- Original property suites exceeded the 30s test timeout under default concurrency.
  A representative property test passes on clean HEAD in ~20s alone. Full final rerun:
  `pnpm test -- --maxWorkers=4 --testTimeout=180000`, same tests/seeds/iteration counts,
  log `/tmp/interleave-i18n-test-final.log`: **460 files / 4932 tests passed** in 571.94s.
- A final invalid-legacy-date fallback was checked by 17 focused tests and a rebuilt
  Electron run (5 passed; pseudo case intentionally skipped). Final lint and all-package
  typecheck passed after this fix. Ordinary bundles and production resource gate passed.
- The packaging rejection was exercised with `INTERLEAVE_I18N_TEST=1`; it stopped
  before packaging, as expected. No package, push, publish or commit was performed.
- `git diff --check` passed. Reviewed IPC ordering, resource isolation, fallback,
  migration SQL, historical rows/source links, undo selection and production gates.
- Developer guide: `docs/internationalization.md` includes research, contribution steps,
  lifecycle behavior and migration inventory.

No Windows/macOS native runtime or installer validation has been claimed. Existing
Windows paths/native-addon and macOS menu/signing branches retain focused tests.
Existing non-failing warnings include the large Vite chunk and a long timer fixture's
`TimeoutOverflowWarning`; neither is counted as a missing check or a platform runtime proof.

Local preview started with `pnpm dev` at `http://localhost:5173/settings`; the Electron
window uses isolated `/tmp/interleave-i18n-preview-QvdaH9` data. The browser URL is the
renderer preview only. The development environment has no vendored EmbeddingGemma model
and reports its existing CPU/keyword fallback; this is unrelated to language resources.
